name: realm_named_generator
description: >-
             Custom generator for RealmObjects, which uses named parameters for required fields
             Also generates builders for each RealmObject

version: 1.0.1

repository: https://github.com/Neuromersive/realm_named_generator

environment:
  sdk: ^3.0.2

dependencies:
  analyzer: ^5.0.0
  build_resolvers: ^2.0.9
  build: ^2.0.0
  dart_style: ^2.2.0
  realm_common: ^1.2.0
  source_gen: ^1.1.0
  source_span: ^1.8.0

dev_dependencies:
  build_test: ^2.1.4

  lints: ^2.0.0
  test: ^1.14.3
  path: ^1.0.0
